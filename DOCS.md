---
skills_used:
  - github:github
  - github:yeet
  - browser:control-in-app-browser
model_used: GPT-5
model_source: visible_runtime
created_at: 2026-08-03T00:00:00+08:00
updated_at: 2026-08-04T16:03:06+08:00
updated_by: codex
---

## 2026-08-04 第二十三次线上真实评估：模型接入与自然类型覆盖

- 环境：提交 `1a0e18f` 已推送到 `violesea/AI-Commute` 的 `agent/travel-planning` 并部署到 `http://192.168.1.56:3002/`；远端 migrate、Web、scheduler、Telegram 均正常运行。
- 入口：根路径显示“模型与接入设置”；`/settings` 可展开 `DeepSeek V4 Flash`、`DeepSeek V4 Pro`，旅行固定 `deepseek-v4-flash`。通勤接入测试 592ms、旅行接入测试 752ms 均成功，API Key 和 Base URL 未进入页面。
- 实跑：会话 `cmsed6m330001mi0s1oto2uxa`，行程 `cmsedbvcz002kmi0squ50wwpe`；状态分别为 `completed` / `monitoring`。请求为 2026-08-08 至 2026-08-12，北京梵谷水郡往返锡林郭勒盟，2 人 1 车，每日自驾不超过 6 小时，只安排白天驾驶，优先草原、湖泊、湿地、山地/火山并兼顾元上都遗址、贝子庙。
- 路线：11 个 stops、10 个连续 legs；10/10 段高德路线匹配；D1-D5 自驾 327/256/121/158/330 分钟，均低于 360 分钟；结构化自驾 1192 分钟、缓冲 150 分钟、合计 1342 分钟。
- 内容：草原、湖泊、湿地、山地、火山 5 类自然类型和 2 个人文景点全部进入主路线；4 个住宿、4 个美食、预算 `2550-3380 元`、7 条避坑、自驾/公共交通比较均可见。`ISSUE-035` 关闭。
- 天气：当前预报截止 2026-08-07，行程 8 月 8-12 日全部显示未知并要求刷新；10 条逐段风险、12 个天气刷新任务、50 个路线复查任务和 10 个出发提醒任务落盘。未来刷新任务只代表排程，不代表未来天气已获取。
- 重试：首次两次 `create_trip` 分别因火山类型覆盖证据和路线端点连续性被拒，第三次修正成功；没有半成品行程。复杂请求重试成本仍由 `ISSUE-010`、`ISSUE-015`、`ISSUE-017` 跟踪。

## 2026-08-04 自然景观优先与多地点天气补丁（已线上回归）

- 真实旧部署证据：北京→锡林郭勒 5 天请求已完成结构化落盘，但自然景观清单出现住宿名称“锡林郭勒宾馆”，且自然推荐中只有少数进入主路线；天气总览只显示单一城市。
- 修复：当请求包含“优先自然风光”时，四天及以上行程要求至少 4 个自然景点进入 stops/legs 主路线；住宿名称误归类为自然景观直接拒绝并要求替换；详情页按实际 route stops 展示线路天气覆盖点。
- 天气口径：`ensureTravelPlanWeatherLocations` 只把天气工具返回的 `weather.city` 视为已查询，不信任模型自行填写的 `forecast[].location` 或 `locations[].status`；其他经过点明确标为 `refresh_required`。
- 本地证据：旅行计划、旅行集成和旅行卡片定向测试 35/35；`npm run lint` 通过；`npm run build` 通过；`git diff --check` 通过。全量测试 60/62 个测试文件、511/513 个断言通过；剩余 Node TLS CA 能力差异和分享 PNG 降级 spy 两项与本补丁无关。
- 线上证据：提交 `c7e14b9` 已推送到 `violesea/AI-Commute` 的 `agent/travel-planning` 并重建 `.56:3002`；migrate 正常退出，Web、scheduler、telegram 均运行。
- 真实回归：会话 `cmse5q6n00001p80sfpym7c0a`、行程 `cmse5srqw001qp80s9gwu766f` 完成；13 个 stops、12 个连续自驾 legs，D1-D5 自驾 225/297/63/315/210 分钟，均低于 360 分钟；5 个自然景点、1 个人文景点、住宿、美食、预算、8 条避坑、自驾/公共交通比较和 12 条路段天气风险可见。
- 天气证据：当前高德预报截止 `2026-08-07`，8 月 8-12 日均按未知处理；详情页按实际路线生成 11 个天气覆盖点，仅天气工具返回的“北京市”显示“已查询”，其余点显示“待刷新”；模型填写的路线标签不再作为查询证据。首段 72/24 小时和各路段出发前 1 小时刷新任务继续保留。
- 设置页：线上 `/settings` 可展开 DeepSeek V4 Flash/Pro，旅行规划固定 `deepseek-v4-flash`，接入测试入口可见，API Key 和 Base URL 未显示。

## 2026-08-04 自然景观类型校验补丁（待线上回归）

- 失败证据：线上会话 `cmsdvzo3w00gjo10r35o5ianm` 使用旧部署创建旅行计划时，模型把自然景点 `naturalType` 全部返回为 `other`；服务端只识别到 1 种自然类型，重复修正后终止，`tripId=null`，无半成品行程。
- 修复：旅行工具 schema 约束 `naturalType` 为 canonical English labels；系统提示明确要求 `lake`、`wetland`、`grassland`、`mountain`、`river`、`volcanic` 等类型并禁止 `other`/`unknown`；服务端同时从名称、理由和备注识别中英文类型，错误反馈包含机器可读修正指令。
- 本地证据：旅行计划单元测试 16 个、规划错误测试 4 个、旅行集成测试 11 个、Agent Chat Client 测试 9 个均通过；生产构建通过，类型检查在构建完成后单独执行通过。
- 全量测试边界：61 个测试文件中 59 个通过，497 个断言中 495 个通过；系统 CA 测试依赖当前 Node 不存在的 `tls.setDefaultCACertificates`，分享 PNG 降级测试未触发下载 spy，均与本补丁无关。
- 待验证：部署后重新运行北京→锡林郭勒自驾请求，确认自然景点进入路线或明确标为备选，并检查天气动态刷新、自驾/公共交通、住宿、美食、预算和避坑。

## 2026-08-04 路线事实一致性补丁（已线上复测）

- 根因：无年份日期输入（如“8月8日至12日”）无法进入日期范围校验；同时旅行创建路径把 stop ID 按数组位置绑定，却保留模型文本中的非相邻起终点。
- 修复：按北京时间当前年份解析中文/数字无年份日期；结构化旅行行程要求每个相邻 stop 只有一段 leg，端点不一致时返回带修正方向的工具错误；旅行创建与替换统一从 canonical stop 生成起终点字段。
- 保持边界：普通通勤仍允许“外部出发点→单一目的地”的显式路线；历史已落盘行程不被自动改写。
- 本地证据：`tests/unit/travel-schedule.test.ts`、`tests/integration/create-trip.test.ts`、`tests/integration/travel-planning.test.ts` 定向通过，`npm run lint` 通过。
- 待办：推送 `violesea/AI-Commute` 后重建 `.56:3002`，用无年份北京→锡林郭勒请求验证超 6 小时返程会被拒绝并重试为连续路线。

# Engineering Notes

## 旅行计划数据

旅行模式通过 Agent 的 `create_trip` 写入结构化 `travelPlan`。天气包含预报、路段风险、动态监控标记和刷新策略；交通同时保存自驾与公共交通证据；景点、住宿、美食、避坑和预算由同一计划保存。

## 旅行时间轴

当用户给出日期范围但没有具体时刻时，服务端根据 `D1`、`Day1`、`第 1 天` 等路线标记重建路线段的北京时区时间，默认日间出发，返程日提前出发，并将停靠点到达时间和提醒任务一起更新。模型给出的异常跨日时间不能直接进入监控系统。用户明确提出“每天自驾不超过 N 小时”时，服务端按自然日累计全部自驾路段，不按单段分别放行；超限创建会把累计分钟数返回给模型，要求拆分路线或增加住宿。

## 天气刷新任务

旅行首段出发前约 72 小时和 24 小时各生成一个 `weather_refresh` 任务；每个旅行路段再生成一个出发前 1 小时的 `weather_refresh` 任务。scheduler 将其作为旅行路线复查运行，先刷新天气再决定是否更新路线。普通通勤仍只使用既有 `recheck` 和 `depart_now` 任务。

旅行详情页显示最近天气刷新时间和当前可用预报截止日期。创建或更新旅行计划时，服务端从当前会话最近一次 `get_weather_reference` 工具调用补齐天气来源、`observedAt` 和缺失的预报；没有足够证据的日期仍显示为未知并要求出发前刷新。

## 推荐证据与核验状态

景点、住宿和美食都支持 `evidence`：来源可为 `amap_poi`、`amap_route`、`amap_weather`、`agent_inference` 或 `user_input`。应用会从当前会话已完成的 POI 工具调用匹配推荐名称并补齐高德参考时间；未匹配时默认显示“AI建议，出发前核验”。高德来源也只代表检索参考，开放时间、价格、预约和临时管制仍需出发前核对。

## 模型配置与接入验证

设置页的模型下拉框只控制普通通勤规划使用的模型；旅行规划在服务端固定使用 `deepseek-v4-flash`，避免用户误选通勤模型后改变旅行规划契约。模型选项通过 `src/lib/agent/model-config.ts` 集中维护，并由设置 API 校验。

“测试当前模型接入”调用 `/api/settings/test-model`。接口要求登录，拒绝未支持模型；服务器未配置 `OPENAI_API_KEY` 时返回 `not_configured`，已配置时使用 OpenAI-compatible Chat Completions 发起一次无工具最小请求，成功返回模型和耗时，失败只返回截断且脱敏后的错误。API Key 和 Base URL 永不进入页面响应。

时间轴计算不会盲信模型的 `totalMinutes`：当它与路线分钟和明确缓冲的差值超过 2 小时时，按路线证据重新计算，避免把公共交通对照耗时带入自驾腿；同日累计超出午夜的路段会顺延到下一可用旅行日，最后仍由跨午夜安全断言拦截不可执行安排。

## 验证

完整验证命令见 README。真实运行验收必须检查数据库中的旅行计划、路线段日期、提醒任务类型和详情页渲染，不能只以首页 HTTP 200 作为成功依据。

## 2026-08-03 线上回归记录

- 环境：`http://192.168.1.56:3002/`，部署提交 `f62136e`。
- 旅行会话：`cmscybkyi0005nw0s36kwhywj`；行程：`cmscyir8f001gnw0s8v75bzvz`；终态为 `completed`，行程状态为 `monitoring`。
- 真实结果：5 个自然景点、1 个人文景点、3 个住宿、3 个美食、预算、避坑、自驾/公共交通对比，天气覆盖 2026-08-08 至 2026-08-11 四天，`weather_refresh` 任务 2 个。
- 由于高德在 2026-08-03 的预报窗口只覆盖 08-03 至 08-06，四个旅行日均按未知处理，并要求出发前刷新；这条状态已通过继续对话和数据库读取再次核验。
- 发现的问题：D3 达里湖返回锡林浩特安排到 21:00，详情页已展示夜路风险；见 `ISSUES.md` 的 ISSUE-006。
- 模型配置：设置页可见通勤模型选择和旅行固定 `deepseek-v4-flash` 说明；`/api/settings/test-model` 实测连通约 1.07 秒，页面未泄露凭据。

## 2026-08-03 第二次线上回归

- 环境：`http://192.168.1.56:3002/`，部署提交 `fbe33de`。
- 旅行会话：`cmsd1dpy00003pf0s0h472wvw`；行程：`cmsd1nhna001mpf0simhzs0wl`；会话终态为 `completed`，行程状态为 `monitoring`。
- 真实结果：5 个自然景点、2 个人文景点、3 个住宿、4 个美食、预算、避坑、自驾/公共交通对比，天气覆盖 2026-08-08 至 2026-08-11 四天，`weather_refresh` 任务 2 个。
- 当前预报只覆盖到 08-06，08-08 至 08-11 均按未知处理；详情页同时展示基线天气、路线风险、出发前 72/24 小时刷新和每段出发前复查要求。
- 时间轴：10 段路线均在请求日期范围内且无跨午夜；D1 07:00 出发、D4 06:30 返程。独立 `suncalc` 复算显示 10/10 段自驾均早于当地日落前 30 分钟安全线。
- 模型配置：线上 `/settings` 实测可展开通勤模型选项（GPT-4o mini、DeepSeek V4 Flash、DeepSeek V4 Pro），页面明确显示旅行固定使用 `deepseek-v4-flash`；接入测试按钮和 API 配置状态可见，页面未显示 API Key 或 Base URL。

## 2026-08-03 第三次线上回归：当前版本

- 环境：`http://192.168.1.56:3002/`，本次从本地提交 `acb6cc7` 同步并重建 Docker 镜像；远端实际使用 `/opt/homebrew/bin/docker-compose`，migrate、web、scheduler、telegram 均启动成功。
- 模型接入：登录后 `/settings` 返回 200，页面可见通勤模型选择和旅行固定 `DeepSeek V4 Flash`；`POST /api/settings/test-model` 返回 `connected`，模型为 `deepseek-v4-flash`，延迟 846ms。
- 旅行会话：`cmsd7ouhf0005s40szjzrvrd8`；行程：`cmsd7vn26001ws40s633rring`；会话终态 `completed`，行程状态 `monitoring`，重试次数 0，工具调用 23 次。
- 旅行结果：路线为北京—正蓝旗上都镇—元上都遗址—锡林浩特—平顶山—达里湖北岸—多伦湖—北京；10 段自驾，日驾驶分钟为 296、223、167、191、327，均不超过用户的 360 分钟上限；路线时间均在 2026-08-08 至 2026-08-12 内，无跨午夜驾驶。
- 内容结果：4 个自然景点（平顶山、达里诺尔湖、锡林河湿地、多伦湖）和 2 个人文景点（元上都遗址、贝子庙）；4 个住宿、4 个美食、预算 `4200-5800 元（2 人 1 车，5 天 4 晚）`、8 个避坑；自驾和公共交通方案均落盘。
- 天气结果：动态监控开启，10 个路段风险落盘；当前高德预报只到 `2026-08-06`，8 月 8-12 日按未知处理；12 个 `weather_refresh` 任务落盘（首段 72/24 小时、10 段路段各 1 小时）。
- 质量观察：模型主动识别达里湖北岸属于赤峰克什克腾旗，提醒“若严格要求全程不出盟”应改用上都湖等替代点；这条边界提醒保留在避坑中，当前路线不是严格的“全程锡林郭勒盟内”路线。

## 2026-08-03 第四次线上回归：北京→锡林郭勒盟

- 环境：`http://192.168.1.56:3002/`，登录后 `/settings` 返回 200；浏览器实测可见通勤模型下拉框、旅行固定 `DeepSeek V4 Flash`、两个接入测试按钮和 `接入成功` 状态。
- 模型接入：`POST /api/settings/test-model` 对 `deepseek-v4-flash` 返回 `connected`，本次延迟 774ms；页面没有显示 API Key 或 Base URL。
- 旅行会话：`cmsd89e0b006rs40szjzrvrd8`；行程：`cmsd8m73d0094s40spdix6icy`；会话终态 `completed`，行程状态 `monitoring`，最终工具调用 26 次。
- 路线结果：北京梵谷水郡—多伦湖—元上都遗址—平顶山火山地质公园—锡林浩特—锡林河湿地—白音锡勒草原—上都河湿地—太仆寺旗—北京；12 段自驾，日期覆盖 2026-08-08 至 2026-08-12，无跨午夜驾驶。
- 硬约束：按北京时间自然日累计自驾为 353、255、145、228、268 分钟，均不超过 360 分钟；独立用 `suncalc` 复核 12/12 段均通过当地日落前 30 分钟安全线。
- 内容结果：6 个自然景点，覆盖湖泊、火山、湿地、草原和森林；2 个人文景点；3 个住宿；4 个美食；预算 `4000-5600 元（2 人 1 车，4 晚）`；8 条避坑。推荐景点均在锡林郭勒盟境内，计划明确把达里诺尔湖、乌兰布统列为盟外扩展，不混入当前路线。
- 天气结果：动态监控开启；当前高德预报边界为 `2026-08-06`，8 月 8-12 日全部按未知处理；详情页展示 5 条代表性自驾路段天气风险；数据库落盘 14 个 `weather_refresh` 任务（12 段路线各 1 个，加首段 72/24 小时）。
- 复杂度观察：请求从启动到完成接近 10 分钟，模型连续尝试多次 `create_trip` 后才通过结构化校验。功能结果可用，但复杂五日旅行的响应耗时需要单独优化，见 `ISSUES.md` 的 ISSUE-010。

## 2026-08-03 第五次线上回归：北京→锡林郭勒盟

- 环境：`http://192.168.1.56:3002/`，本次修复提交 `beb7946` 已同步并重建 Docker 镜像；migrate、web、scheduler、telegram 均启动成功。
- 设置页：登录后浏览器实测可见 GPT-4o mini、DeepSeek V4 Flash、DeepSeek V4 Pro 通勤模型选项；旅行规划固定显示 `deepseek-v4-flash`；通勤和旅行接入测试分别成功，耗时 884ms、867ms；页面未显示 API Key 或 Base URL。
- 首次会话 `cmsda1w6p0003pc0r1mxt59um` 在旧部署上因模型使用 `Day1` 标记而触发日期范围错误，经过 28 次工具调用后停止。修复 `DayN` 标记解析并重新部署后，第二次会话 `cmsdary680001o00sfm1de71o` 以 `completed` 结束，行程 `cmsdb4f1x001wo00sffzvsekf` 状态为 `monitoring`。
- 路线结果：13 段自驾，覆盖北京梵谷水郡、元上都遗址、正蓝旗、多伦湖、锡林浩特、平顶山火山、西乌珠穆沁草原 99 号公路、锡林河湿地、贝子庙、南山森林和返京；所有路线段在 2026-08-08 至 2026-08-12 内，无跨午夜驾驶。
- 硬约束：北京时间每日自驾分钟为 D1 351、D2 226、D3 301、D4 192、D5 285，均低于 360 分钟；详情页显示所有到达早于日落前 30 分钟安全线。
- 内容结果：9 个自然景点，覆盖草原、湖泊、火山、湿地、森林、沙地疏林草原；2 个人文景点；2 个住宿、3 个美食、预算 `约4200-6500元（2人1车）`、5 条避坑；自驾与公共交通方案均落盘，公共交通北京→锡林浩特约 744 分钟，自驾去程约 311 分钟。
- 天气结果：动态监控开启；5 个行程日天气均标记为当前预报范围外未知，当前高德预报截止 `2026-08-06`；13 个自驾路段风险全部落盘；15 个 `weather_refresh` 任务落盘，包括首段 72/24 小时任务和 13 段路线各 1 小时任务。
- 性能观察：第二次会话从创建到完成约 9 分 42 秒，25 次工具调用、3 次 `create_trip`（前两次分别被天气风险覆盖和日落安全线校验拒绝）。结构化错误已可诊断且未产生半成品行程，但复杂旅行响应耗时仍由 ISSUE-010 跟踪。

## 2026-08-03 第六次线上回归：北京→锡林郭勒盟

- 环境：`http://192.168.1.56:3002/`，线上部署提交 `2a77005`；设置页模型选择和通勤/旅行模型接入测试均已在此前回归通过。
- 旅行会话：`cmsdcetoy0001up0rsljaj7vw`；行程：`cmsdcmhoi002cup0r3vwpk3w5`；会话页面显示 `agent已完成规划`，行程详情进入监控状态。
- 实跑输入：北京→锡林郭勒盟，2026-08-08 至 2026-08-12，2 人 1 车，每日自驾不超过 6 小时，只安排白天驾驶，优先自然风光并兼顾人文，景点限定在盟内，盟外仅作明确备选。
- 路线结果：13 段自驾；D1-D5 结构化驾驶分钟为 262、209、278、179、267，均低于 360 分钟上限；详情页路线段均在日期范围内且未跨午夜，目标到达 8 月 12 日 11:27。
- 内容结果：6 个自然景点（贡宝拉格草原、上都湖、浑善达克沙地、多伦湖、平顶山、锡林九曲湾），3 个人文景点（元上都遗址、汇宗寺、贝子庙），3 个住宿、6 个美食、预算和 6 条避坑；公共交通对比显示约 742 分钟，给出自驾优先理由。
- 天气结果：当前高德预报截止 `2026-08-06`，行程 8 月 8-12 日全部明确标记为未知；13/13 路段天气风险均有独立记录，详情页显示动态监控、首段 72/24 小时刷新和每段出发前 1 小时刷新，提醒任务共 93 个。
- 质量发现：D4 贝子庙推荐停留 90 分钟，但线上详情的显式路线在 08:15 到达后立即进入返程；本地已增加显式时间轴停留时长校验和回归测试，见 `ISSUES.md` 的 ISSUE-012。摘要称合计驾驶 1168 分钟，而结构化路线段合计 1195 分钟，见 ISSUE-013。
- 发布状态：实跑使用 `2a77005`；修复后提交 `eb544f7` 已推送到 `violesea/AI-Commute` 并在 `192.168.1.56:3002` 重建运行。修复只影响后续新建或替换路线，不改写已落盘的本次旧行程。

## 2026-08-04 第七次线上回归：北京→锡林郭勒盟

- 环境：`http://192.168.1.56:3002/`，部署提交 `7d01bb6`；Web、scheduler、telegram 容器均正常运行。
- 模型与入口：登录后 `/settings` 浏览器实测显示 GPT-4o mini、DeepSeek V4 Flash、DeepSeek V4 Pro 三个通勤模型选项；页面显示“服务器 API 已配置”、当前模型接入测试、旅行固定 `DeepSeek V4 Flash` 和旅行模型接入测试。根路径 `/` 只显示“模型与接入设置”入口卡片。
- 旅行会话：`cmsdf65c80003mp0te8knt67e`；行程：`cmsdfcr1f0028mp0ttkw88qq7`；会话终态 `completed`，行程状态 `monitoring`。模型日志记录 6 次旅行模型调用；工具调用 30 次，`create_trip` 首次因非法 JSON 失败，第二次成功；会话从 16:04:16 至 16:09:24 UTC，端到端约 5 分 08 秒。
- 实跑输入：北京梵谷水郡出发并返回，北京→锡林郭勒盟，2026-08-08 至 2026-08-12，2 人 1 车，每日自驾不超过 6 小时，只安排白天驾驶，优先自然风光并兼顾人文，要求比较自驾与公共交通。
- 路线事实：11 段自驾；北京时间 D1-D5 结构化驾驶分钟为 299、187、64、222、309，均不超过 360 分钟；详情页显示 8 月 8 日 07:00 出发、8 月 12 日 06:30 返程、目标到达 12:10，无跨午夜驾驶。页面结构化路线事实显示自驾 1081 分钟、缓冲 165 分钟、合计 1246 分钟。
- 内容结果：6 个自然景点（草原、火山、河流、湿地、沙地、湖泊）和 2 个人文景点（贝子庙、元上都遗址），4 个住宿建议、4 个美食建议、约 `3800-4900 元` 预算、8 条避坑；高德 POI 证据和 AI 推断项均保留相应核验状态。
- 天气结果：动态监控开启，5 个行程日天气条目和 11 条路段风险落盘；高德当前可用预报只到 `2026-08-07`，8 月 8-12 日全部按未知处理；79 个提醒任务落盘，其中 13 个 `weather_refresh`（首段 72/24 小时和 11 段路线各 1 小时）、55 个 `recheck`、11 个 `depart_now`。
- 质量观察：本次响应耗时较此前约 9 分 42 秒有所下降，但仍受模型结构化 JSON 重试影响，ISSUE-010 继续开放；详情页路线段主时间显示正确，但说明文本仍有“按行程结构化时间”占位语句，见 ISSUE-014。

## 2026-08-04 第八次线上回归：北京→锡林郭勒盟

- 环境：`http://192.168.1.56:3002/`，远端镜像按提交 `3bfe439` 重建；Web、scheduler、telegram 已重新创建并启动。
- 模型接入：`/settings` 可展开 GPT-4o mini、DeepSeek V4 Flash、DeepSeek V4 Pro 三个通勤模型；旅行固定使用 `deepseek-v4-flash`。旅行模型测试页面返回“接入成功”，耗时 1108ms；页面未泄露 API Key 或 Base URL。
- 真实会话：`cmsdi149w0001pd0tomcbz4mw`；行程 `cmsdi4hbw001qpd0td84ev5xx`；会话 `completed`，行程 `monitoring`。
- 真实内容：10 段自驾，D1-D5 为 307、170、45、170、287 分钟，全部低于每日 360 分钟；无跨午夜驾驶。6 个自然景点覆盖草原、湖泊、火山地质、河流湿地、湿地 5 类，3 个人文景点，4 个住宿、4 个美食、预算和 7 条避坑均显示。
- 动态天气：详情页显示高德当前预报只到 `2026-08-07`，8 月 8-12 日标记为未知；10 条路段天气风险、动态监控、首段 72/24 小时及每段出发前 1 小时刷新任务共 12 个均已显示。
- 失败保护：第一次 `create_trip` 缺少结构化 `travelPlan` 被拒，第二次补齐后成功；没有半成品行程落盘。该重试成本新增 `ISSUE-015`，性能优化仍由 `ISSUE-010` 跟踪。

## 2026-08-04 第九次线上回归：北京→锡林郭勒盟

- 环境：`http://192.168.1.56:3002/`，真实运行提交 `5961a33`；Web、scheduler、telegram 容器均正常运行。
- 模型与入口：浏览器实测首页有“模型与接入设置”入口；`/settings` 可展开 GPT-4o mini、DeepSeek V4 Flash、DeepSeek V4 Pro 三个通勤模型；旅行固定使用 `deepseek-v4-flash`。旅行模型接入测试返回“接入成功”，耗时 904ms。页面未显示 API Key 或 Base URL。
- 旅行会话：`cmsdk2t180001oa0ssvx2if9p`；行程：`cmsdk6qqt001soa0s7t8pdvmh`；会话终态 `completed`，行程状态 `monitoring`。22 次工具调用、7 次旅行模型调用；前两次 `create_trip` 分别因非法 JSON 和停靠点日期字段为 `D1` 被拒，第三次成功，未落盘半成品。
- 路线事实：9 段自驾；D1-D5 结构化自驾分钟为 436、139、316、232、431；路线无跨午夜驾驶。详情页显示自驾 1554 分钟、缓冲 235 分钟、合计 1789 分钟，并以结构化路线事实覆盖模型摘要。
- 内容结果：5 个自然景点，覆盖火山/山景、草原、湿地/河流、湖泊/湿地和森林/公园；2 个人文景点；4 个住宿、4 个美食、预算 `5600-7200 元（两人一车）`、8 条避坑。自驾与公共交通对比均落盘。
- 天气结果：动态监控开启；高德当前可用预报截止 `2026-08-07`，8 月 8-12 日全部按未知处理；9 条路段天气风险和 11 个 `weather_refresh` 任务（首段 72/24 小时加 9 段路线各 1 小时）落盘；详情页明确展示出发前刷新要求。
- 质量结论：自然景点机制已经能产出 5 类自然类型，但推荐清单与实际 stops 不完全一致，锡林河国家湿地公园、南山森林公园、贝子庙未进入路线段，登记 `ISSUE-016`。本地已增加 `D1/Day1/第1天` 旅行日标记兼容，待部署后复测。

## 2026-08-04 第十次线上回归：北京→锡林郭勒盟

- 当前线上提交：`dcd47a4`；真实会话 `cmsdmnsxw0001qn0r85h4yinq` 创建行程 `cmsdmqcgw001qqn0r0ov83bx0`，终态 `completed`，行程进入 `monitoring`。
- 设置页：通勤模型和旅行固定模型接入测试分别返回成功，耗时 958ms、1018ms；页面不显示 API Key 或 Base URL。
- 路线：9 段自驾，D1-D5 为 438、122、232、80、429 分钟；无跨午夜，长途去返均有白天出发、服务区休息和返程风险提示。
- 内容：4 类自然景观进入实际路线，另有 1 个人文景点；4 个住宿、4 个美食、预算、8 条避坑和公共交通对比均可见。
- 天气：预报截止 2026-08-07，8 月 8-12 日全部显示未知；9 条路段天气风险和 11 个天气刷新任务可见，动态监控开启。
- 待优化：推荐清单仍包含未进入 stops 的南山森林公园和贝子庙；首次 `create_trip` 因缺少 `travelPlan.transport` 重试一次，分别见 `ISSUE-016`、`ISSUE-015`。

## 2026-08-04 第十一次线上回归：北京→锡林郭勒盟

- 部署提交：`b2c4e74`，远端四个容器均正常启动。该提交新增 `travelPlan` 推荐数组扁平化兼容；数组缺失时只从同一工具调用的顶层 `attractions`、`lodging`、`food`、`pitfalls` 归并，嵌套数组优先。
- 本地验证：旅行计划单元测试 13/13、旅行集成测试 10/10、串行类型检查和生产构建通过。
- 实测会话：`cmsdoazqt0001pp0sbmm0tpwi`，DeepSeek V4 Flash，27 次工具调用、4 次 `create_trip`，约 225 秒后 `failed`；`tripId=null`，没有半成品行程。
- 失败链：首次工具参数非法 JSON；第二次扁平推荐数组已被兼容并继续校验，但一条美食缺少 `mustTry`；第三次只保留部分 `travelPlan`，缺少 `destination`；第四次完整计划被白天驾驶安全校验拒绝，D1 多伦湖→锡林九曲预计 18:59 到达，安全线为 18:54。
- 可靠结论：天气当前可用预报截止 2026-08-07，行程日全部应按未知并要求刷新；安全校验有效且没有错误落盘，但 DeepSeek 在连续错误修正中仍不能稳定保留完整结构并重排时间，见 `ISSUE-017`。

## 2026-08-04 第十二次线上回归：北京→锡林郭勒盟

- 环境：`http://192.168.1.56:3002/` 浏览器实测；目标会话 `cmsdqayr50001ox0rh44o5c37`，最终行程 `cmsdqe4ol001mox0rq51epo1v`，会话显示 `agent已完成规划`，行程监控已开启。
- 实跑输入：2026-08-08 至 2026-08-12，北京梵谷水郡出发并返回，2 人普通燃油车，每日自驾不超过 6 小时，只安排白天驾驶；优先自然风光并兼顾人文，景点限定在锡林郭勒盟内，盟外仅作明确备选；要求动态天气、逐段风险、自驾/公共交通比较、住宿、美食、预算和避坑。
- 路线事实：10 段自驾；北京时间 D1-D5 为 300、175、125、170、300 分钟，均低于 360 分钟上限；详情页显示 8 月 8 日 07:00 出发、8 月 12 日 06:30 返京，路线日期均在请求范围内且无跨午夜驾驶。结构化路线事实为自驾 1070 分钟、缓冲 120 分钟、合计 1190 分钟。
- 内容结果：6 个景点全部标记“已安排进路线”，其中 4 个自然景观（湖泊、火山地质、河流草原、河流湿地）和 2 个人文景点（元上都遗址、贝子庙）；4 个自然类型多样性和景点与 stops 的一致性本轮通过。另有 2 个住宿建议、3 个美食建议、约 `4200-5500 元` 预算和 7 条避坑。
- 天气结果：动态监控开启；5 个行程日全部提示天气未知，当前高德可用预报截止 `2026-08-07`；10 条路段天气风险和 12 个 `weather_refresh` 任务可见（首段 72/24 小时、10 段路线各 1 小时）。未来刷新任务已排程，尚未到执行时间，不能把排程当成已刷新天气。
- 模型与对话：会话中完成设置、天气、自然景观、POI、驾车和公共交通取证，POI 预算耗尽后停止搜索，随后一次 `create_trip` 成功落盘；当前代码固定旅行规划使用 `deepseek-v4-flash`，通勤模型入口仍在 `/settings`。
- 质量发现：第 4 天地图显示“住宿→湿地→多伦”，但分段明细的第二段仍从“锡林浩特住宿”出发，未显式表达湿地返回住宿或湿地直接南下的连接，登记 `ISSUE-018`。该问题不影响本轮每日 6 小时硬约束，但会影响路线可执行性和分钟数解释。

## 2026-08-04 第十三次线上回归：连续路线修复

- 环境：`http://192.168.1.56:3002/`，部署提交 `c59a531`；Web、scheduler、telegram 容器均正常运行。
- 旅行会话：`cmsds0yjw001tlg0s92r62v7w`；行程 `cmsds7g68004elg0sm5s0y7tk`；会话终态 `completed`，行程状态 `monitoring`。首次独立尝试因 DeepSeek `Premature close` 失败且未落盘，重试成功。
- 路线事实：8 个 stops、7 个自驾 legs，起终点均按相邻 stops 连续映射；D1-D5 自驾分钟为 226、173、165、215、307，均不超过 360 分钟；无跨午夜驾驶，去程拆分为太仆寺旗，返程拆分为多伦县城。
- 内容事实：6 个景点（4 个自然、2 个人文），4 个住宿、4 个美食、约 4900-6500 元预算、10 条避坑；自驾与公共交通比较均落盘。
- 天气事实：动态监控开启，7 条路段天气风险和 9 个天气刷新任务落盘；当前预报截止 2026-08-07，8 月 8-12 日要求出发前和逐段动态刷新，不能把未来刷新任务当成已执行天气。
- 设置页：浏览器实测可展开 GPT-4o mini、DeepSeek V4 Flash、DeepSeek V4 Pro；旅行固定使用 `deepseek-v4-flash`，旅行模型接入测试成功，耗时 630ms；页面未显示 API Key 或 Base URL。

## 2026-08-04 第十四次线上真实验收：北京→锡林郭勒

- 入口核验：根路径显示“模型与接入设置”卡片；`/settings` 可展开 GPT-4o mini、DeepSeek V4 Flash、DeepSeek V4 Pro。通勤模型测试和旅行固定 `deepseek-v4-flash` 测试均返回“接入成功”；API Key 与 Base URL 未出现在页面。
- 实跑请求：2026-08-08 至 2026-08-12，北京梵谷水郡往返锡林郭勒盟，2 人燃油车，每日自驾不超过 6 小时，仅白天驾驶，优先自然风光并兼顾人文，要求天气动态、交通比较、住宿、美食、预算和避坑。
- 结果地址：会话 `cmsdtmzn10003qp0szj1j2d40`；行程 `cmsdtr6xg001wqp0s93vnnar5`；详情页为 `/trips/cmsdtr6xg001wqp0s93vnnar5`。
- 结构化路线：11 段自驾，D1-D5 为 342、234、68、168、252 分钟，总自驾 1064 分钟，缓冲 192 分钟，合计 1256 分钟；每日上限和白天驾驶约束通过。
- 动态天气：当前高德预报只覆盖至 2026-08-07，行程 8 月 8-12 日全部显示为未知；页面显示逐段天气风险、动态监控和出发前刷新策略。刷新任务只代表排程，不代表未来天气已经获取。
- 内容结果：5 个实际路线景点、3 个备选景点、4 个住宿建议、4 个美食建议、预算和 8 条避坑；自驾与公共交通均有单独方案和理由。景点清单与 stops 的已安排/备选边界本轮通过。
- 质量发现：天气卡片显示“第 5 天”至“第 9 天”而非本次旅行 D1-D5；一条“每日驾驶限 6 小时”的避坑文本将 D2 路段计入 D1，和结构化 342 分钟事实冲突；博物馆推荐疑似同一 POI 重复出现。见 `ISSUES.md` 的 `ISSUE-021`、`ISSUE-022`。

## 2026-08-04 第十五次线上真实验收：旅行事实一致性修复

- 环境：提交 `781daeb` 已推送到 `violesea/AI-Commute` 的 `agent/travel-planning` 分支，并在 `192.168.1.56:3002` 重建；Web、scheduler、telegram 容器均正常运行。
- 入口与模型：根路径显示“模型与接入设置”；`/settings` 实测可展开 GPT-4o mini、DeepSeek V4 Flash、DeepSeek V4 Pro，旅行固定为 `deepseek-v4-flash`；旅行模型接入测试返回“接入成功”，耗时 732ms，页面未显示 API Key 或 Base URL。
- 实跑请求：2026-08-08 至 2026-08-12，北京梵谷水郡往返锡林郭勒盟，2 人 1 车，每日自驾不超过 6 小时，只安排白天驾驶，优先自然风光并兼顾人文，要求天气动态、交通比较、住宿、美食、预算和避坑。
- 线上证据：会话 `cmsdutv2n0001o10r5h6qcxe0`，行程 `cmsduxe2t001oo10rra095zty`；会话 `completed`，行程 `monitoring`；数据库落盘 14 个 stops、13 个连续 legs，未产生半成品行程。
- 结构化路线：北京时间 D1-D5 自驾分钟为 300、221、75、260、332，均不超过 360 分钟；总自驾 1188 分钟，缓冲 250 分钟，合计 1438 分钟；数据库级 stops/legs 连续性校验通过。
- 天气与提醒：5 个行程日天气均显示为超出当前预报范围、未知；当前高德预报截止 2026-08-07；动态监控开启，15 个 `weather_refresh` 任务真实落盘，另有逐段复查和出发提醒。
- 内容结果：8 个景点全部进入路线，其中 5 个自然景点（火山、湿地、森林、湖泊、沙地）和 3 个人文景点；5 个住宿、3 个美食、预算和 10 条避坑均可见。每日自驾避坑文案与结构化分钟一致，景点别名重复已合并。
- 质量结论：天气日序、每日驾驶事实和重复 POI 本轮通过；未来天气刷新任务仍只是排程，不代表已获取未来天气，价格和开放信息仍需临期核验。

## 2026-08-04 第十六次线上真实验收：自然景观类型修复

- 环境：提交 `cffa250` 已推送到 `violesea/AI-Commute` 的 `agent/travel-planning` 分支并部署到 `http://192.168.1.56:3002/`；Web、scheduler、telegram 容器正常运行。
- 设置页：`/settings` 浏览器实测可展开 `GPT-4o mini`、`DeepSeek V4 Flash`、`DeepSeek V4 Pro`；通勤与旅行接入测试均成功，旅行固定为 `deepseek-v4-flash`；页面未显示 API Key 或 Base URL。
- 实跑输入：2026-08-15 至 2026-08-19，北京梵谷水郡往返锡林郭勒盟，2 人、普通燃油车，仅白天驾驶，每天自驾不超过 6 小时；优先草原、湖泊、湿地、山地/火山等自然景观，同时要求人文、天气、交通对比、住宿、美食、预算和避坑。
- 线上证据：会话 `cmsdwv8a80001mf0se5optjqz`，行程 `cmsdwzdda001smf0stmj63tdn`；会话 `completed`，行程 `monitoring`。21 次工具调用中有 1 次非法 JSON 的 `create_trip` 失败，重试后成功，未产生半成品。
- 结构化路线：10 个 stops、9 个连续 legs；北京时间 D1-D5 自驾 330、268、208、234、242 分钟，全部不超过 360 分钟；去程和返程均经中途住宿拆分，无跨午夜驾驶。详情页显示结构化自驾 1282 分钟、缓冲 185 分钟、合计 1467 分钟。
- 天气结果：当前高德可用预报截止 2026-08-07，8 月 15-19 日 5 天全部显示未知并要求刷新；动态监控开启，9 条路段风险和 11 个 `weather_refresh` 任务落盘。未来刷新任务只代表排程，不能视为已获取的逐日天气。
- 内容结果：旅行计划包含火山、湿地、湖泊和草原四类自然类型，以及人文景点；4 个住宿、4 个美食、约 `5800-7000 元` 预算和 6 条避坑可见；自驾优先理由与公共交通约 771 分钟对照均可见。
- 质量结论：自然类型阻断已解决并关闭 `ISSUE-023`。但页面将实际路线上的太仆寺旗/张北草原误显示为备选，部分天气风险标题与真实 leg 起终点不一致，火山景点跨日住宿/返城连接不明确；见 `ISSUE-024`、`ISSUE-025`、`ISSUE-026`。当前结果适合评估功能闭环，不适合直接作为无人工复核的最终出行单。

## 2026-08-04 第十七次线上真实验收：北京往返锡林郭勒

- 环境：提交 `4c1b9c5` 已推送到 `violesea/AI-Commute` 的 `agent/travel-planning` 并部署到 `http://192.168.1.56:3002/`；远端 Web、scheduler、telegram 容器均正常运行。
- 入口与模型：根路径显示“模型与接入设置”；登录后 `/settings` 可展开 `GPT-4o mini`、`DeepSeek V4 Flash`、`DeepSeek V4 Pro`，通勤模型测试返回成功，旅行固定模型测试返回成功；页面未显示 API Key 或 Base URL。
- 实跑请求：2026-08-15 至 2026-08-19，北京梵谷水郡往返锡林郭勒盟，2 人、普通燃油车，仅白天驾驶，每天自驾不超过 6 小时；优先草原、湖泊、湿地、山地/火山等自然景观，同时要求人文、天气、交通对比、住宿、美食、预算和避坑。
- 线上证据：会话 `cmsdy5rlo0001s50se479p0gd`，行程 `cmsdya07m0028s50spxu23syn`；会话 `completed`，行程 `monitoring`；最终路线 11 个 stops、10 个连续 legs，创建阶段两次超限结构化请求被拒后由 DeepSeek V4 Flash 修正成功，未产生半成品行程。
- 路线事实：北京时间 D1-D5 自驾分钟为 311、237、179、174、271，均不超过 360 分钟；总自驾 1172 分钟，缓冲 130 分钟，合计 1302 分钟；路线日期在请求范围内且无跨午夜驾驶。
- 天气与提醒：当前高德可用预报截止 `2026-08-07`，8 月 15-19 日全部按预报窗口外未知处理；10 个路段均有风险条目，动态监控开启，12 个天气刷新任务、50 个路线复查任务和 10 个出发提醒任务落盘。刷新任务只代表排程，不代表未来天气已获取。
- 内容结果：实际覆盖草原、火山和湿地 3 个自然景点，湖泊“上都湖”列为备选；元上都遗址、贝子庙 2 个人文景点已安排；4 个住宿、5 个美食、预算 `4580-5900 元（2 人）`、8 条避坑以及自驾/公共交通比较均可见。
- 本轮修复：交通卡片不再展示模型自报的“15.5 小时”，线上详情页统一显示“按已落盘路线，自驾 1172 分钟（约 19.5 小时）”；对应 `ISSUE-027` 关闭。
- 质量结论：天气动态边界、每日驾驶硬约束、连续路线、结构化提醒和模型接入闭环通过；湖泊未进入主路线、个别美食疑似重复或区域错配，分别由 `ISSUE-028`、`ISSUE-029` 跟踪。当前结果可用于评估功能，不应作为未经临期核验的最终出行单。

## 2026-08-04 第十八次线上真实评估：DeepSeek V4 Flash 与天气驱动路线

- 环境：提交 `42a898b` 已推送到 `violesea/AI-Commute` 的 `agent/travel-planning` 并在 `http://192.168.1.56:3002/` 重建；`migrate`、Web、scheduler、telegram 均已运行。
- 模型配置：由于远端使用 DeepSeek 官方 Base URL，设置页按 Provider 只显示 Flash/Pro 两个通勤选项；旅行固定使用 `deepseek-v4-flash`。通勤接入测试成功（2127ms），旅行接入测试成功（5969ms）；测试期间一次 503 上游繁忙在刷新后恢复。API Key 和 Base URL 未进入页面。
- 真实行程：会话 `cmse1jc8l0001th0stw0npp5h`，行程 `cmse1le160022th0sx4qhoays`，详情页为 `http://192.168.1.56:3002/trips/cmse1le160022th0sx4qhoays`；会话 `completed`，行程 `monitoring`。
- 路线验证：11 个 stops、10 个 legs，端点按相邻 stops 连续；D1-D5 自驾分别为 348、240、229、155、333 分钟，最大 348 分钟；总自驾 1305 分钟、缓冲 155 分钟、合计 1460 分钟。独立运行时区、日期范围和 `suncalc` 日落安全线校验，10/10 段通过。
- 内容验证：4 个自然景点进入主路线，覆盖湖泊、火山、湿地、草原；森林公园标为备选；元上都遗址进入主路线；4 个住宿、4 个美食、预算、7 条避坑、自驾/公共交通比较和 10 条逐段天气风险均可见。
- 天气验证：当前高德预报截止 `2026-08-07`，8 月 8-12 日均显示未知；动态监控开启，12 个天气刷新任务落盘。详情页能显示逐段风险和具体驾驶动作，但天气总览显示“张家口市 暂无天气信息”，而 stops 实际覆盖北京、正蓝旗、锡林浩特和达里诺尔，已登记 `ISSUE-030`。
- 结论：模型接入和旅行结构化闭环可用，自然景观主路线覆盖优于前几轮；内容仍需把多地天气总览、门票/开放时间和未来天气临期核验纳入最终出行前检查。

## 2026-08-04 第十九次线上真实评估：北京→锡林郭勒

- 入口与模型：根路径显示“模型与接入设置”卡片；登录后 `/settings` 显示通勤模型选择和两项接入测试。本次浏览器实测通勤接入成功（706ms），旅行接入成功（843ms）。因当前远端 Base URL 为 DeepSeek 官方地址，线上通勤选项为 `DeepSeek V4 Flash`、`DeepSeek V4 Pro`；旅行规划固定为 `deepseek-v4-flash`，页面未显示 API Key 或 Base URL。
- 真实请求：2026-08-06 出发、5 天 4 晚，北京出发并返回锡林郭勒盟，2 人 1 车，每日自驾不超过 6 小时，仅白天驾驶，优先自然风光并兼顾人文，同时要求天气、交通、住宿、美食、预算和避坑。
- 线上证据：会话 `cmse37piz0001oe0rnern312a`，行程 `cmse3brsc0022oe0rhtxso8hf`；会话 `completed`，行程 `monitoring`；详情页为 `http://192.168.1.56:3002/trips/cmse3brsc0022oe0rhtxso8hf`。
- 硬约束：8 个 stops、7 个连续 legs；D1-D5 自驾 326/162/252/168/339 分钟，全部低于 360 分钟，无跨午夜驾驶；结构化自驾 1247 分钟、缓冲 153 分钟、合计 1400 分钟。
- 天气与提醒：当前高德预报截止 2026-08-07，8 月 8-10 日均显示未知；7 条逐段风险和 9 个天气刷新任务落盘，动态监控开启。未来刷新是排程，不代表已经获取未来天气。
- 内容：自驾与公共交通对比、4 个住宿、4 个美食、约 `4200-5600 元`预算、7 条避坑均可见；4 个自然推荐中仅达里诺尔湖和平顶山进入主路线，草原和湿地为备选，元上都遗址进入主路线、贝子庙为备选。
- 结论：单一出发日期加“5 天 4 晚”不再绕过路线安全校验；模型接入、天气未知边界和结构化行程闭环通过。自然偏好到实际主路线停靠的转化仍不足，新增 `ISSUE-031`；该行程可用于功能评估，不应未经临期核验直接作为最终出行单。

## 2026-08-04 第二十次线上评估：每日驾驶反馈修复后的北京→锡林郭勒

- 环境：提交 `58d243e` 已推送到 `violesea/AI-Commute` 的 `agent/travel-planning` 并部署到 `http://192.168.1.56:3002/`；远端 Docker 入口为 `/opt/homebrew/bin/docker-compose`，migrate、Web、scheduler、Telegram 均正常运行。
- 入口与模型：根路径显示“模型与接入设置”卡片；登录后 `/settings` 显示通勤模型选择、当前模型接入测试和旅行模型接入测试。通勤测试成功 998ms，旅行固定 `deepseek-v4-flash` 测试成功 821ms；API Key 和 Base URL 未显示。
- 实跑请求：2026-08-08 至 2026-08-12，北京梵谷水郡出发并返回锡林郭勒盟，2 人 1 车，每日自驾不超过 6 小时，只安排白天驾驶；优先草原、湖泊、湿地、火山/山地等自然景观，同时要求人文、天气、交通、住宿、美食、预算和避坑。
- 线上证据：会话 `cmse7zsqd0001ml0s8svv756u`，行程 `cmse84np0001mml0spnem3xtb`；会话 `completed`，行程 `monitoring`；11 个 stops、10 个连续 legs，未产生半成品。
- 硬约束：结构化 D1-D5 自驾为 333、307、95、175、333 分钟，全部不超过 360 分钟；日期在请求范围内，无跨午夜；详情页总自驾 1243 分钟、缓冲 110 分钟、合计 1353 分钟。
- 内容结果：洪格尔草原天路、达里诺尔湖、平顶山火山地质公园、锡林河国家湿地公园进入主路线；元上都遗址和贝子庙进入主路线；4 个住宿、4 个美食、预算 `5200—6500 元（2 人）`、避坑和自驾/公共交通比较可见。乌拉盖九曲湾因单程约 320 分钟列为需增加夜晚的备选。
- 天气结果：当前高德预报截止 `2026-08-07`，8 月 8-12 日均为预报窗口外未知；10 条路段天气风险和 12 个天气刷新任务落盘，线路多地天气点中仅实际查询的锡林郭勒盟标为“已查询”，其他点待刷新。未来任务是排程，不代表已获取行程期天气。
- 失败反馈验证：第一次 `create_trip` 因 `travelPlan.pitfalls` 缺失被拒，第二次成功；本次未触发每日驾驶超限。此前同一类超限现在会返回逐段分钟诊断，但结构化首次重试成本仍由 `ISSUE-015` 跟踪。
- 新质量发现：该会话只记录 4 次 `get_driving_route`，却落盘 10 个自驾 leg，部分景区/市区短驳分钟缺少独立路线证据；登记 `ISSUE-033`。当前结果适合评估功能闭环，不应未经天气、门票、开放时间、房态、价格和路线证据临期核验直接出行。

## 2026-08-04 第二十一次线上真实评估：安全景观取舍后的北京→锡林郭勒

- 部署与运行：`96358af` 已部署到 `http://192.168.1.56:3002/`；远端 `migrate`、Web、scheduler、Telegram 容器均正常运行。数据库读取与浏览器页面结果一致。
- 模型入口：根路径显示“模型与接入设置”卡片；登录后 `/settings` 的通勤模型下拉可展开 `DeepSeek V4 Flash`、`DeepSeek V4 Pro`，测试按钮分别验证当前通勤模型和固定旅行模型。浏览器实测返回“接入成功：DeepSeek V4 Flash”，通勤 832ms、旅行 840ms；API Key 与 Base URL 未返回页面。由于远端使用 DeepSeek 官方 Base URL，线上不显示 GPT-4o mini 是 Provider 过滤结果，不是下拉失效。
- 真实会话：请求为 2026-08-08 至 2026-08-12，北京梵谷水郡往返锡林郭勒盟，2 人 1 车，每日自驾不超过 6 小时，仅白天驾驶，优先草原/湖泊/湿地/火山或山地，并要求人文、天气、交通、住宿、美食、预算和避坑。会话 `cmseakw0z0001qo0s93j1oi0b` 状态 `completed`，行程 `cmsean90w0024qo0sqcxjc6fa` 状态 `monitoring`。
- 路线硬约束：13 个 stops、12 个 legs，数据库校验所有 leg 端点均绑定相邻 stops；D1-D5 自驾 259/284/95/215/334 分钟，均低于 360 分钟；结构化自驾 1187 分钟、缓冲 231 分钟、合计 1418 分钟。用行程坐标和 `suncalc` 独立复核 12/12 段均在当地日落前 30 分钟安全线前到达。
- 景点结果：4 个自然景点进入主路线，覆盖火山、湿地、草原、湖泊；贝子庙、汇宗寺 2 个人文点进入主路线；姑娘湖和元上都遗址明确列为备选，不计入路线覆盖。4 个住宿、4 个美食、约 `4800-6000 元`预算和 7 条避坑均可见。
- 天气结果：高德当前可用预报截止 `2026-08-07`，8 月 8-12 日均按“超出预报范围、出发前刷新”处理；动态监控开启，12 条路段风险和 14 个 `weather_refresh` 任务落盘。14 个任务由首段 72/24 小时刷新各 1 个和 12 段出发前 1 小时刷新组成；排程不是未来天气已获取的证明。
- 质量边界：只有 4/12 段有高德路线证据，8/12 段为模型估算，已加入 131 分钟安全余量并在页面提示临期用地图替换，继续跟踪 `ISSUE-033`。结构化路线为 1187 分钟，但保存的交通对比字段仍为模型自报 1336 分钟；详情页已使用 1187 分钟，原始计划一致性登记 `ISSUE-034`。
- 结论：安全景观取舍修复通过真实回归，用户点名的四类自然景观进入主路线且没有牺牲每日驾驶上限和白天驾驶安全线。该行程适合评估功能闭环，不应未经临期天气、门票、开放时间、房态、价格及路线证据核验直接出行。

## 2026-08-04 第二十二次线上真实评估：路线证据补查与交通时长归一化

- 当前线上入口：`http://192.168.1.56:3002/`；当前旅行模型由 `read_settings` 实际返回 `deepseek-v4-flash`。
- 真实会话：`cmsec1t4u0003pd0sdekq46wc`，行程 `cmsec8zlz002wpd0skjp0rvmq`，终态分别为 `completed` / `monitoring`；详情页为 `http://192.168.1.56:3002/trips/cmsec8zlz002wpd0skjp0rvmq`。
- 路线：10 个 stops、9 个 legs；D1-D5 自驾 247/149/262/244/279 分钟，结构化总自驾 1181 分钟、缓冲 75 分钟、合计 1256 分钟，均未超过每日 360 分钟且无跨午夜驾驶。
- 路线证据：本次执行 23 次 `get_driving_route`，9/9 个自驾 leg 均为高德 `provider_verified`；没有模型估算段，安全余量为 0。`ISSUE-033` 已关闭。
- 交通字段：模型初始请求的 1169 分钟被服务端归一化为结构化 1181 分钟；持久化 `travelPlan.transport.driving.durationMinutes`、`summary`、详情页交通卡片和路线事实一致。`ISSUE-034` 已关闭。
- 重试行为：首个 `create_trip` 因 D1 自驾 373 分钟超过 360 分钟被拒，第二次按错误明细拆分后成功；没有半成品行程。
- 内容：4 个自然景点进入主线，覆盖草原、湖泊、火山、湿地；元上都遗址和贝子庙进入主线，乌兰五台为明确的山地备选；4 个住宿、4 个美食、约 `5000-6800 元`预算和 11 条避坑可见。
- 天气：高德预报当前截止 2026-08-07，行程 8/8-8/12 全部为未知；9 条逐段风险和首段 72/24 小时、每段出发前 1 小时刷新任务落盘。刷新任务是排程，不是未来天气已获取证明。
- 遗留质量：主路线实际覆盖草原、湖泊、火山、湿地，山地乌兰五台为备选；详情页“5 个点名自然类型均已进入主路线”的文字与 stops 冲突，见 `ISSUE-035`。
