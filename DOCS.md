---
skills_used:
  - github:github
  - browser:control-in-app-browser
model_used: GPT-5
model_source: visible_runtime
created_at: 2026-08-03T00:00:00+08:00
updated_at: 2026-08-03T23:10:44+08:00
updated_by: codex
---

# Engineering Notes

## 旅行计划数据

旅行模式通过 Agent 的 `create_trip` 写入结构化 `travelPlan`。天气包含预报、路段风险、动态监控标记和刷新策略；交通同时保存自驾与公共交通证据；景点、住宿、美食、避坑和预算由同一计划保存。

## 旅行时间轴

当用户给出日期范围但没有具体时刻时，服务端根据 `D1`、`Day1`、`第 1 天` 等路线标记重建路线段的北京时区时间，默认日间出发，返程日提前出发，并将停靠点到达时间和提醒任务一起更新。模型给出的异常跨日时间不能直接进入监控系统。用户明确提出“每天自驾不超过 N 小时”时，服务端按自然日累计全部自驾路段，不按单段分别放行；超限创建会把累计分钟数返回给模型，要求拆分路线或增加住宿。

## 天气刷新任务

旅行首段出发前约 72 小时和 24 小时各生成一个 `weather_refresh` 任务；每个旅行路段再生成一个出发前 1 小时的 `weather_refresh` 任务。scheduler 将其作为旅行路线复查运行，先刷新天气再决定是否更新路线。普通通勤仍只使用既有 `recheck` 和 `depart_now` 任务。

旅行详情页显示最近天气刷新时间和当前可用预报截止日期。创建或更新旅行计划时，服务端从当前会话最近一次 `get_weather_reference` 工具调用补齐天气来源、`observedAt` 和缺失的预报；没有足够证据的日期仍显示为未知并要求出发前刷新。

## 推荐证据与核验状态

景点、住宿和美食都支持 `evidence`：来源可为 `amap_poi`、`amap_route`、`amap_weather`、`agent_inference` 或 `user_input`。应用会从当前会话已完成的 POI 工具调用匹配推荐名称并补齐高德参考时间；未匹配时默认显示“AI建议，出发前核验”。高德来源也只代表检索参考，开放时间、价格、预约和临时管制仍需出发前核对。

## 模型配置与接入验证

设置页的模型下拉框只控制普通通勤规划使用的模型；旅行规划在服务端固定使用 `deepseek-v4-flash`，避免用户误选通勤模型后改变旅行规划契约。模型选项通过 `src/lib/agent/model-config.ts` 集中维护，并由设置 API 校验。

“测试当前模型接入”调用 `/api/settings/test-model`。接口要求登录，拒绝未支持模型；服务器未配置 `OPENAI_API_KEY` 时返回 `not_configured`，已配置时使用 OpenAI-compatible Chat Completions 发起一次无工具最小请求，成功返回模型和耗时，失败只返回截断且脱敏后的错误。API Key 和 Base URL 永不进入页面响应。

时间轴计算不会盲信模型的 `totalMinutes`：当它与路线分钟和明确缓冲的差值超过 2 小时时，按路线证据重新计算，避免把公共交通对照耗时带入自驾腿；同日累计超出午夜的路段会顺延到下一可用旅行日，最后仍由跨午夜安全断言拦截不可执行安排。

## 验证

完整验证命令见 README。真实运行验收必须检查数据库中的旅行计划、路线段日期、提醒任务类型和详情页渲染，不能只以首页 HTTP 200 作为成功依据。

## 2026-08-03 线上回归记录

- 环境：`http://192.168.1.56:3002/`，部署提交 `f62136e`。
- 旅行会话：`cmscybkyi0005nw0s36kwhywj`；行程：`cmscyir8f001gnw0s8v75bzvz`；终态为 `completed`，行程状态为 `monitoring`。
- 真实结果：5 个自然景点、1 个人文景点、3 个住宿、3 个美食、预算、避坑、自驾/公共交通对比，天气覆盖 2026-08-08 至 2026-08-11 四天，`weather_refresh` 任务 2 个。
- 由于高德在 2026-08-03 的预报窗口只覆盖 08-03 至 08-06，四个旅行日均按未知处理，并要求出发前刷新；这条状态已通过继续对话和数据库读取再次核验。
- 发现的问题：D3 达里湖返回锡林浩特安排到 21:00，详情页已展示夜路风险；见 `ISSUES.md` 的 ISSUE-006。
- 模型配置：设置页可见通勤模型选择和旅行固定 `deepseek-v4-flash` 说明；`/api/settings/test-model` 实测连通约 1.07 秒，页面未泄露凭据。

## 2026-08-03 第二次线上回归

- 环境：`http://192.168.1.56:3002/`，部署提交 `fbe33de`。
- 旅行会话：`cmsd1dpy00003pf0s0h472wvw`；行程：`cmsd1nhna001mpf0simhzs0wl`；会话终态为 `completed`，行程状态为 `monitoring`。
- 真实结果：5 个自然景点、2 个人文景点、3 个住宿、4 个美食、预算、避坑、自驾/公共交通对比，天气覆盖 2026-08-08 至 2026-08-11 四天，`weather_refresh` 任务 2 个。
- 当前预报只覆盖到 08-06，08-08 至 08-11 均按未知处理；详情页同时展示基线天气、路线风险、出发前 72/24 小时刷新和每段出发前复查要求。
- 时间轴：10 段路线均在请求日期范围内且无跨午夜；D1 07:00 出发、D4 06:30 返程。独立 `suncalc` 复算显示 10/10 段自驾均早于当地日落前 30 分钟安全线。
- 模型配置：线上 `/settings` 实测可展开通勤模型选项（GPT-4o mini、DeepSeek V4 Flash、DeepSeek V4 Pro），页面明确显示旅行固定使用 `deepseek-v4-flash`；接入测试按钮和 API 配置状态可见，页面未显示 API Key 或 Base URL。

## 2026-08-03 第三次线上回归：当前版本

- 环境：`http://192.168.1.56:3002/`，本次从本地提交 `acb6cc7` 同步并重建 Docker 镜像；远端实际使用 `/opt/homebrew/bin/docker-compose`，migrate、web、scheduler、telegram 均启动成功。
- 模型接入：登录后 `/settings` 返回 200，页面可见通勤模型选择和旅行固定 `DeepSeek V4 Flash`；`POST /api/settings/test-model` 返回 `connected`，模型为 `deepseek-v4-flash`，延迟 846ms。
- 旅行会话：`cmsd7ouhf0005s40szjzrvrd8`；行程：`cmsd7vn26001ws40s633rring`；会话终态 `completed`，行程状态 `monitoring`，重试次数 0，工具调用 23 次。
- 旅行结果：路线为北京—正蓝旗上都镇—元上都遗址—锡林浩特—平顶山—达里湖北岸—多伦湖—北京；10 段自驾，日驾驶分钟为 296、223、167、191、327，均不超过用户的 360 分钟上限；路线时间均在 2026-08-08 至 2026-08-12 内，无跨午夜驾驶。
- 内容结果：4 个自然景点（平顶山、达里诺尔湖、锡林河湿地、多伦湖）和 2 个人文景点（元上都遗址、贝子庙）；4 个住宿、4 个美食、预算 `4200-5800 元（2 人 1 车，5 天 4 晚）`、8 个避坑；自驾和公共交通方案均落盘。
- 天气结果：动态监控开启，10 个路段风险落盘；当前高德预报只到 `2026-08-06`，8 月 8-12 日按未知处理；12 个 `weather_refresh` 任务落盘（首段 72/24 小时、10 段路段各 1 小时）。
- 质量观察：模型主动识别达里湖北岸属于赤峰克什克腾旗，提醒“若严格要求全程不出盟”应改用上都湖等替代点；这条边界提醒保留在避坑中，当前路线不是严格的“全程锡林郭勒盟内”路线。

## 2026-08-03 第四次线上回归：北京→锡林郭勒盟

- 环境：`http://192.168.1.56:3002/`，登录后 `/settings` 返回 200；浏览器实测可见通勤模型下拉框、旅行固定 `DeepSeek V4 Flash`、两个接入测试按钮和 `接入成功` 状态。
- 模型接入：`POST /api/settings/test-model` 对 `deepseek-v4-flash` 返回 `connected`，本次延迟 774ms；页面没有显示 API Key 或 Base URL。
- 旅行会话：`cmsd89e0b006rs40szjzrvrd8`；行程：`cmsd8m73d0094s40spdix6icy`；会话终态 `completed`，行程状态 `monitoring`，最终工具调用 26 次。
- 路线结果：北京梵谷水郡—多伦湖—元上都遗址—平顶山火山地质公园—锡林浩特—锡林河湿地—白音锡勒草原—上都河湿地—太仆寺旗—北京；12 段自驾，日期覆盖 2026-08-08 至 2026-08-12，无跨午夜驾驶。
- 硬约束：按北京时间自然日累计自驾为 353、255、145、228、268 分钟，均不超过 360 分钟；独立用 `suncalc` 复核 12/12 段均通过当地日落前 30 分钟安全线。
- 内容结果：6 个自然景点，覆盖湖泊、火山、湿地、草原和森林；2 个人文景点；3 个住宿；4 个美食；预算 `4000-5600 元（2 人 1 车，4 晚）`；8 条避坑。推荐景点均在锡林郭勒盟境内，计划明确把达里诺尔湖、乌兰布统列为盟外扩展，不混入当前路线。
- 天气结果：动态监控开启；当前高德预报边界为 `2026-08-06`，8 月 8-12 日全部按未知处理；详情页展示 5 条代表性自驾路段天气风险；数据库落盘 14 个 `weather_refresh` 任务（12 段路线各 1 个，加首段 72/24 小时）。
- 复杂度观察：请求从启动到完成接近 10 分钟，模型连续尝试多次 `create_trip` 后才通过结构化校验。功能结果可用，但复杂五日旅行的响应耗时需要单独优化，见 `ISSUES.md` 的 ISSUE-010。

## 2026-08-03 第五次线上回归：北京→锡林郭勒盟

- 环境：`http://192.168.1.56:3002/`，本次修复提交 `beb7946` 已同步并重建 Docker 镜像；migrate、web、scheduler、telegram 均启动成功。
- 设置页：登录后浏览器实测可见 GPT-4o mini、DeepSeek V4 Flash、DeepSeek V4 Pro 通勤模型选项；旅行规划固定显示 `deepseek-v4-flash`；通勤和旅行接入测试分别成功，耗时 884ms、867ms；页面未显示 API Key 或 Base URL。
- 首次会话 `cmsda1w6p0003pc0r1mxt59um` 在旧部署上因模型使用 `Day1` 标记而触发日期范围错误，经过 28 次工具调用后停止。修复 `DayN` 标记解析并重新部署后，第二次会话 `cmsdary680001o00sfm1de71o` 以 `completed` 结束，行程 `cmsdb4f1x001wo00sffzvsekf` 状态为 `monitoring`。
- 路线结果：13 段自驾，覆盖北京梵谷水郡、元上都遗址、正蓝旗、多伦湖、锡林浩特、平顶山火山、西乌珠穆沁草原 99 号公路、锡林河湿地、贝子庙、南山森林和返京；所有路线段在 2026-08-08 至 2026-08-12 内，无跨午夜驾驶。
- 硬约束：北京时间每日自驾分钟为 D1 351、D2 226、D3 301、D4 192、D5 285，均低于 360 分钟；详情页显示所有到达早于日落前 30 分钟安全线。
- 内容结果：9 个自然景点，覆盖草原、湖泊、火山、湿地、森林、沙地疏林草原；2 个人文景点；2 个住宿、3 个美食、预算 `约4200-6500元（2人1车）`、5 条避坑；自驾与公共交通方案均落盘，公共交通北京→锡林浩特约 744 分钟，自驾去程约 311 分钟。
- 天气结果：动态监控开启；5 个行程日天气均标记为当前预报范围外未知，当前高德预报截止 `2026-08-06`；13 个自驾路段风险全部落盘；15 个 `weather_refresh` 任务落盘，包括首段 72/24 小时任务和 13 段路线各 1 小时任务。
- 性能观察：第二次会话从创建到完成约 9 分 42 秒，25 次工具调用、3 次 `create_trip`（前两次分别被天气风险覆盖和日落安全线校验拒绝）。结构化错误已可诊断且未产生半成品行程，但复杂旅行响应耗时仍由 ISSUE-010 跟踪。

## 2026-08-03 第六次线上回归：北京→锡林郭勒盟

- 环境：`http://192.168.1.56:3002/`，线上部署提交 `2a77005`；设置页模型选择和通勤/旅行模型接入测试均已在此前回归通过。
- 旅行会话：`cmsdcetoy0001up0rsljaj7vw`；行程：`cmsdcmhoi002cup0r3vwpk3w5`；会话页面显示 `agent已完成规划`，行程详情进入监控状态。
- 实跑输入：北京→锡林郭勒盟，2026-08-08 至 2026-08-12，2 人 1 车，每日自驾不超过 6 小时，只安排白天驾驶，优先自然风光并兼顾人文，景点限定在盟内，盟外仅作明确备选。
- 路线结果：13 段自驾；D1-D5 结构化驾驶分钟为 262、209、278、179、267，均低于 360 分钟上限；详情页路线段均在日期范围内且未跨午夜，目标到达 8 月 12 日 11:27。
- 内容结果：6 个自然景点（贡宝拉格草原、上都湖、浑善达克沙地、多伦湖、平顶山、锡林九曲湾），3 个人文景点（元上都遗址、汇宗寺、贝子庙），3 个住宿、6 个美食、预算和 6 条避坑；公共交通对比显示约 742 分钟，给出自驾优先理由。
- 天气结果：当前高德预报截止 `2026-08-06`，行程 8 月 8-12 日全部明确标记为未知；13/13 路段天气风险均有独立记录，详情页显示动态监控、首段 72/24 小时刷新和每段出发前 1 小时刷新，提醒任务共 93 个。
- 质量发现：D4 贝子庙推荐停留 90 分钟，但线上详情的显式路线在 08:15 到达后立即进入返程；本地已增加显式时间轴停留时长校验和回归测试，见 `ISSUES.md` 的 ISSUE-012。摘要称合计驾驶 1168 分钟，而结构化路线段合计 1195 分钟，见 ISSUE-013。
- 发布状态：实跑使用 `2a77005`；修复后提交 `eb544f7` 已推送到 `violesea/AI-Commute` 并在 `192.168.1.56:3002` 重建运行。修复只影响后续新建或替换路线，不改写已落盘的本次旧行程。
