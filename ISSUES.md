---
skills_used:
  - github:github
  - github:yeet
  - browser:control-in-app-browser
model_used: GPT-5
model_source: visible_runtime
created_at: 2026-08-03T00:00:00+08:00
updated_at: 2026-08-04T11:28:53+08:00
updated_by: codex
---

# Issues

## ISSUE-001 · feature · P1 · done

旅行线路规划基础能力：天气参考、自驾与公共交通对比、自然/人文景点、住宿、美食和避坑。

验收：旅行模式创建结构化 `travelPlan`；自然景观四日行程至少四个；天气缺少未来预报时明确标注未知；路线监控可继续复查。

验证：2026-08-03 在 `http://192.168.1.56:3002/` 实际创建行程 `cmscyir8f001gnw0s8v75bzvz`，会话 `cmscybkyi0005nw0s36kwhywj` 终态为 `completed`。数据库真实落盘 5 个自然景点、1 个人文景点、4 个行程日天气条目、3 个住宿、3 个美食、预算、避坑、自驾/公共交通对比和 2 个 `weather_refresh` 任务。天气预报窗口不覆盖 8 月 8-11 日时，四天均明确写为未知并要求出发前刷新。

追加验证：2026-08-04 当前部署真实创建会话 `cmsdi149w0001pd0tomcbz4mw`，行程 `cmsdi4hbw001qpd0td84ev5xx` 终态为 `completed`，行程状态为 `monitoring`。页面落盘 6 个自然景点、3 个人文景点、4 个住宿、4 个美食、预算、7 条避坑、10 段自驾和 12 个天气刷新任务；每日自驾为 307、170、45、170、287 分钟，全部低于 360 分钟。天气当前仅覆盖到 8 月 7 日，8 月 8-12 日均标记为待刷新。

## ISSUE-002 · bug · P1 · done

旅行路线的日期和时间轴可能被模型写成跨日或倒序时间，导致长途驾驶安排不可执行。

验收：给出日期范围但未给出时刻时，系统生成的所有路线段按日历日递增；单段出发早于到达；不出现跨午夜驾驶；页面显示日期和时间；返程长途不晚于安全时段。

## ISSUE-003 · feature · P1 · done

旅行规划缺少总预算和费用分项。

验收：结构化计划包含总预算、油费/过路费、住宿、餐饮、门票及补能等分项；详情页可读展示；未知价格明确标注待核实。

## ISSUE-004 · feature · P1 · done

旅行天气策略只写在文案中，没有落成出发前提醒任务。

验收：旅行行程创建后，在首段出发前约 72 小时和 24 小时生成天气刷新任务；任务进入 scheduler；详情页标注为天气刷新；路线复查后不丢失这些任务。

## ISSUE-005 · feature · P1 · done

管理设置页缺少可见的模型接入验证，用户无法区分模型下拉框存在和服务端真的可调用。

验收：设置页可选择通勤模型；明确显示旅行规划固定使用 `deepseek-v4-flash`；点击“测试当前模型接入”后由服务器使用环境变量发起最小请求；页面不显示 API Key；未配置或请求失败时返回可读状态。

验证：模型测试 API 5/5、设置页 UI 76/76；本次部署后线上 `/settings` 实测显示“通勤规划模型”和“旅行规划模型：DeepSeek V4 Flash”，`/api/settings/test-model` 返回 `connected`，`deepseek-v4-flash` 实际连通 846ms；页面未显示 API Key 或 Base URL。

追加验证：2026-08-04 浏览器实测可展开 3 个通勤模型选项（GPT-4o mini、DeepSeek V4 Flash、DeepSeek V4 Pro）；旅行模型固定为 `deepseek-v4-flash`，旅行接入测试返回“接入成功”，耗时 1108ms。页面未显示 API Key 或 Base URL。

## ISSUE-006 · quality · P1 · done

旅行模型在用户要求长途驾驶安排在白天时，仍可能安排较长的日内夜间返程。

证据：真实行程 D3 的“达里湖景区（北岸）→锡林浩特住宿”安排为 18:30-21:00，路线风险也标记为“夜间路段”；该段约 150km，模型已自行提示疲劳、牲畜和夜路风险，但没有自动拆分住宿或给出明确的白天替代方案。

验收：当用户要求白天驾驶时，系统对日落后仍在途的较长自驾段给出可执行的提前返程、途中住宿或缩短景点方案；详情页不把高风险夜间返程呈现为默认的“节奏适中”。

验证：2026-08-03 在最新部署 `fbe33de` 上重新规划北京→锡林郭勒 4 天自驾行程，会话 `cmsd1dpy00003pf0s0h472wvw` 终态为 `completed`，行程 `cmsd1nhna001mpf0simhzs0wl` 进入 `monitoring`。系统将原先可能导致夜间返程的安排拆成 10 段，详情页显示 D1 07:00 出发、D4 06:30 返程；独立用 `suncalc` 复算，10/10 段自驾均早于当地日落前 30 分钟安全线到达。

## ISSUE-007 · feature · P1 · done

旅行后续路段没有独立的出发前天气刷新任务，天气策略容易停留在首段 72/24 小时提醒或文案中。

验收：每个旅行路段在出发前 1 小时生成 `weather_refresh` 任务；首段额外生成出发前 72 小时和 24 小时任务；创建、路线替换、提醒重建和 scheduler 复查后均不丢失；详情页显示天气最近刷新时间和预报覆盖范围。

验证：旅行集成测试和设置/旅行组件回归通过；`create_trip`、路线替换和提醒重建路径复用每段路段的 1 小时刷新规则；真实行程 `cmsd7vn26001ws40s633rring` 生成 12 个 `weather_refresh` 任务（首段 72/24 小时加 10 段路线各 1 小时）；详情页显示当前可用预报截至 `2026-08-06`，未知日期不会伪装成已覆盖。

## ISSUE-008 · feature · P1 · done

景点、住宿和美食推荐缺少证据来源与待核实标记，模型生成内容容易被误读为已验证事实。

验收：结构化推荐支持高德 POI、AI 推断、用户提供等来源；缺少来源时默认标记为“AI建议，出发前核验”；详情页展示来源、核验状态和天气数据最近刷新时间；创建与路线更新时可从已记录的工具调用补齐来源。

验证：旅行计划归一化、工具证据补齐和详情页 UI 回归通过；真实行程落盘 6 个景点（4 自然、2 人文）、4 个住宿、4 个美食，其中 POI 推荐均带 `amap_poi`，AI 推断项仍标记出发前核验；开放时间、价格、预约和临时管制仍保留出发前核验提示。

## ISSUE-009 · bug · P1 · done

模型按多个自驾分段规划时，可能只检查单段时长，导致同一自然日累计驾驶超过用户明确给出的上限。

验收：用户提出“每天自驾不超过 N 小时”时，服务端按北京时间自然日累计所有自驾路段；超限的 `create_trip` 不落盘并把累计时长和上限返回给模型，模型必须拆分日期、增加住宿、缩短路线或删减景点后重试。

验证：`parseDailyDrivingLimitMinutes`、累计超限断言和服务级重规划测试通过；6.5 小时方案先被拒绝，拆分到下一天后才允许落盘。线上真实行程 `cmsd7vn26001ws40s633rring` 的每日自驾为 296、223、167、191、327 分钟，均低于 360 分钟上限。

## ISSUE-010 · performance · P1 · open

复杂旅行请求的结构化落盘重试次数过多，用户需要长时间等待。

证据：2026-08-03 线上真实北京→锡林郭勒盟 5 天 4 晚请求，会话 `cmsd89e0b006rs40szjzrvrd8` 共记录 26 次工具调用，模型在路线和 POI 证据完成后连续尝试多次 `create_trip`，从启动到最终完成接近 10 分钟；最终行程 `cmsd8m73d0094s40spdix6icy` 内容和约束均通过。修复 `DayN` 时间轴标记后再次实测会话 `cmsdary680001o00sfm1de71o`，25 次工具调用、3 次 `create_trip`，端到端约 9 分 42 秒；两次结构化失败均被诊断后才成功落盘，耗时问题仍未解决。

追加证据：提交 `cffa250` 部署后的会话 `cmsdwv8a80001mf0se5optjqz` 记录 21 次工具调用、1 次非法 JSON 的 `create_trip` 重试，约 193 秒后成功创建行程 `cmsdwzdda001smf0stmj63tdn`。响应耗时较此前下降，但复杂请求仍不能视为即时完成。

验收：同等复杂度的旅行请求在不降低天气、路线、景点证据和结构化校验质量的前提下，将端到端响应压到可接受范围；结构化校验失败应尽量提供可诊断、低重复的修正路径。

## ISSUE-011 · bug · P1 · done

旅行模型使用 `Day1`、`Day2` 等英文路线标记时，时间轴归一化只识别 `D1` 和“第 1 天”，导致同一天的多个路段被错误顺延，最终可能超出用户日期范围。

验收：`DayN`、`DN` 和“第 N 天”均能按自然日分组；同一天的连续路段不被顺延到下一天；日期范围校验和白天驾驶校验继续生效。

验证：提交 `beb7946` 增加 `DayN` 解析及单元测试；旅行时间轴测试 10/10 通过。线上第二次真实请求 `cmsdary680001o00sfm1de71o` 的 13 段路线全部落在 2026-08-08 至 2026-08-12，D1 至 D5 每日自驾分钟均通过 360 分钟上限和日落安全线。

## ISSUE-012 · bug · P1 · done

显式时间轴没有把景点的 `plannedStayMin` 纳入连续性判断，模型可能把“到达景点”和“离开景点”写成同一时刻，详情页虽然展示了建议停留时间，但实际路线没有留下游览时间。

验收：如果下一段出发时间早于上一段到达时间加目的地停留时长，显式时间轴必须回退到按日程标记重排；已经满足停留时长的显式时间轴继续保留；创建与替换路线均沿用该约束。

验证：2026-08-03 线上实跑会话 `cmsdcetoy0001up0rsljaj7vw`、行程 `cmsdcmhoi002cup0r3vwpk3w5` 的 D4 贝子庙出现该问题（推荐停留 90 分钟，但 08:15 到达后立即作为返程出发时间）。本地修复 `isExplicitScheduleSafe` 并增加回归测试；旅行定向测试 26/26、构建和类型检查通过。

## ISSUE-013 · quality · P2 · done

旅行计划的自然语言摘要可能与结构化路线明细中的驾驶总分钟数不一致，用户无法判断哪个数字是硬数据。

证据：线上行程 `cmsdcmhoi002cup0r3vwpk3w5` 摘要写“合计驾驶约 1168 分钟”，详情页 13 段路线分钟合计为 1195 分钟，D1-D5 日驾驶 262/209/278/179/267 分钟之和同样为 1195 分钟。服务端硬约束使用结构化路线数据，当前硬约束未被绕过，但展示存在 27 分钟差异。

验收：详情页摘要、路线段合计和每日合计使用同一权威数据；模型摘要中的估算数字不能覆盖结构化路线事实。

验证：当前线上行程 `cmsdfcr1f0028mp0ttkw88qq7` 的详情页显示“结构化路线事实”，自驾 1081 分钟、路程 1081 分钟、缓冲 165 分钟、合计 1246 分钟，并显示 D1-D5 为 299、187、64、222、309 分钟；页面明确提示 AI 摘要不覆盖结构化路线分钟数。硬约束和用户可见结果均以该结构化事实为准。

## ISSUE-014 · quality · P2 · open

旅行详情页部分路线说明仍显示“按行程结构化时间”占位语句，影响阅读，但不改变结构化日期、时间和路线约束。

证据：线上行程 `cmsdfcr1f0028mp0ttkw88qq7` 的路线分段标题已正确显示 8 月 8 日 07:00、11:30 等北京时间；同一段说明仍出现“按行程结构化时间-按行程结构化时间游览”等归一化文本。

验收：保留服务端结构化时间作为唯一事实源，同时将路线说明改为实际时间或自然的中性表述，不再向用户展示内部占位短语。

补充证据：2026-08-04 当前实跑中结构化日期和时间均可读，未复现该占位短语；ISSUE-014 仍保留开放，需继续覆盖更多模型输出。

## ISSUE-015 · quality · P2 · open

复杂旅行请求在结构化 `create_trip` 首次参数不完整时仍需模型自行重试，影响首次响应时间。

证据：2026-08-04 会话 `cmsdi149w0001pd0tomcbz4mw` 首次 `create_trip` 因缺少结构化 `travelPlan` 被服务端拒绝，模型第二次才成功创建行程 `cmsdi4hbw001qpd0td84ev5xx`。最新会话 `cmsdk2t180001oa0ssvx2if9p` 共 22 次工具调用、3 次 `create_trip`：第一次返回非法 JSON，第二次把停靠点 `targetArriveAt` 写成 `D1` 后被日期解析拒绝，第三次成功创建 `cmsdk6qqt001soa0s7t8pdvmh`。安全校验正确阻止了半成品落盘；本地已增加对旅行日标记的兼容和更明确的提示，待线上部署后复测重试成本。

追加验证：线上提交 `dcd47a4` 的会话 `cmsdmnsxw0001qn0r85h4yinq` 首次 `create_trip` 因缺少 `travelPlan.transport` 被拒，模型补齐后第二次成功创建行程 `cmsdmqcgw001qqn0r0ov83bx0`；半成品未落盘。旅行日标记兼容已在线生效，但结构化字段缺失仍会产生一次重试，问题继续开放。

追加验证：提交 `b2c4e74` 的会话 `cmsdoazqt0001pp0sbmm0tpwi` 中，模型第二次把四个推荐数组放到 `travelPlan` 外，服务端兼容归并后继续执行校验；该子问题已由本提交覆盖。但后续模型又分别丢失 `mustTry`、完整 `destination` 和可执行的白天时间轴，最终 4 次 `create_trip` 均未落盘，说明应保留上一版完整计划并以机器可读补丁修正，而不是让模型重写整个大对象。

验收：在不放宽旅行计划结构化校验的前提下，减少模型重复构造参数的次数，或提供更明确的机器可读修正反馈。

## ISSUE-016 · quality · P1 · done

旅行计划的推荐景点清单与实际路线 stops 没有强一致性，用户可能把“推荐”误读为“已经安排进路线”。

证据：最新线上行程 `cmsdk6qqt001soa0s7t8pdvmh` 的 `travelPlan.attractions` 包含 5 个自然景点和 2 个人文景点，但路线 stops 只安排了平顶山、锡林郭勒大草原、元上都遗址和达里诺尔湖；锡林河国家湿地公园、南山森林公园、贝子庙没有对应停靠点或路线段。计划摘要仍写“覆盖锡林河湿地公园”和“贝子庙”，详情页也将未排入 stops 的内容显示在推荐清单中。

验收：每个非备选推荐景点必须对应一个路线 stop 和至少一段路线；未安排的推荐必须明确标记为“备选/顺路可选”，并从“本次覆盖”摘要中排除；天气风险、停留时间和提醒任务与实际 stops 保持一致。

追加证据：同一线上行程 `cmsdmqcgw001qqn0r0ov83bx0` 的实际路线已安排平顶山、锡林河九曲湾、达里诺尔湖和锡林郭勒大草原，但推荐清单仍列出未形成独立 stop 的锡林浩特南山森林公园和贝子庙；页面摘要只把已排入路线的 4 个自然景观和 1 个人文景点写入“覆盖”，推荐清单与可执行路线仍需拆分为“已安排”和“备选”。

验证：提交 `fa5b268` 线上行程 `cmsdtr6xg001wqp0s93vnnar5` 已将 5 个实际路线景点列入“本次路线覆盖”，另外 3 个未进入 stops 的景点明确标为“备选，不计入本次覆盖”；详情页景点卡片同步显示“已安排进路线”或“备选 / 顺路可选”。原验收通过。推荐名称去重和模型文案一致性另见 `ISSUE-022`。

## ISSUE-017 · bug · P1 · open

旅行 `create_trip` 校验失败后的重试会丢失完整 `travelPlan`，或没有按白天驾驶错误重排时间，复杂请求最终无法落盘。

证据：2026-08-04 提交 `b2c4e74` 的真实会话 `cmsdoazqt0001pp0sbmm0tpwi` 共 4 次 `create_trip`：第一次非法 JSON；第二次扁平推荐数组归并后暴露 `food[].mustTry` 缺失；第三次仅保留 `travelPlan.attractions`，被拒绝缺少 `destination`；第四次完整计划仍安排 D1 多伦湖→锡林九曲，预计 18:59 到达，晚于 18:54 白天驾驶安全线。会话终态为 `failed`，`tripId=null`，没有半成品行程。

验收：结构化校验失败后，系统保留上一版已通过的 travelPlan 分块，只接受针对错误字段的补丁；涉及日落或每日驾驶上限时，下一次调用必须改变对应 stop、leg 或日期，不能重复同一时间轴。连续修正应在可控耗时内完成，成功或失败原因都要对用户可见。

追加验证：`fa5b268` 线上会话 `cmsdtmzn10003qp0szj1j2d40` 首次 `create_trip` 因模型返回的工具参数不是 JSON 对象而失败，未落盘半成品；重试后创建行程 `cmsdtr6xg001wqp0s93vnnar5` 并通过每日驾驶与白天驾驶校验。安全边界有效，但仍发生一次结构化重试，问题继续开放。

## ISSUE-018 · quality · P2 · done

旅行地图中的连续 stop 与路线分段起点可能不一致，导致用户无法确认中间连接是否已计入驾驶分钟和提醒。

证据：2026-08-04 真实会话 `cmsdqayr50001ox0rh44o5c37`、行程 `cmsdqe4ol001mox0rq51epo1v` 的地图序列为“锡林浩特住宿→锡林河国家湿地公园→多伦住宿”，但详情页第 4 天分段显示“锡林浩特住宿→锡林河国家湿地公园（20 分钟）”后，下一段仍显示“锡林浩特住宿→多伦住宿（150 分钟）”。如果实际需要从湿地返回住宿再南下，返回连接没有作为独立路线落盘；如果模型意图直接从湿地南下，分段起点又是错误的。当前每日结构化驾驶 170 分钟仍低于 360 分钟上限，但路径、风险、总分钟数和提醒存在口径风险。

验收：每一段路线的起点必须等于上一段终点，或显式增加中间连接段；地图序列、路线分段、路段天气风险、驾驶分钟和提醒任务使用同一 stop 链；任何未计入的返程连接必须在详情页明确展示。

验证：提交 `c59a531` 部署到 `192.168.1.56:3002` 后，真实会话 `cmsds0yjw001tlg0s92r62v7w` 创建行程 `cmsds7g68004elg0sm5s0y7tk`。行程为 8 个 stops、7 个 legs；每段 `fromStopId/toStopId` 和名称都对应相邻 stops，地图与路线分段连续性检查通过，未产生半成品行程。

## ISSUE-021 · quality · P2 · done

旅行天气卡片直接展示模型 `forecast.day`，当模型同时返回出发日以前的参考天气时，行程天气会显示为“第 5 天”至“第 9 天”，用户难以按本次旅行的 D1-D5 理解。

证据：线上行程 `cmsdtr6xg001wqp0s93vnnar5` 的旅行日期为 2026-08-08 至 2026-08-12，详情页对应天气卡片显示“第 5 天”至“第 9 天”；同页还保留 8 月 4-7 日的参考天气并显示“第 1 天”至“第 4 天”。`src/components/trips/travel-plan-card.tsx` 当前直接渲染 `forecast.day`。

验收：行程天气按旅行日期显示 D1-D5 或“第 1 天”至“第 5 天”；行程外的基线天气单独标记为参考，不与行程天数混用。

验证：提交 `781daeb` 部署后，线上行程 `cmsduxe2t001oo10rra095zty` 的 2026-08-08 至 2026-08-12 天气卡片显示第 1 天至第 5 天；当前预报范围外条目显示为未知，未将行程外参考天气混入行程日序。

## ISSUE-022 · quality · P1 · done

模型生成的避坑和推荐文本可能与结构化路线事实矛盾，或重复推荐同一地点，详情页当前只做展示没有一致性校验。

证据：`cmsdtr6xg001wqp0s93vnnar5` 的结构化 D1 自驾为 252+60+30=342 分钟，但避坑卡片把 D1 写成 252+60+168=480 分钟并称“D1 已超上限”；其中 168 分钟实际属于 D2 的太仆寺旗→锡林浩特路段。景点清单同时出现“火山地质公园博物馆”和“锡林郭勒草原火山地质公园博物馆”，地址相同，疑似同一 POI 的重复推荐。

验收：涉及每日驾驶、白天驾驶和路线覆盖的自然语言文案必须以结构化 stops/legs/daily stats 为事实源；矛盾文案应被修正或降级为待核验提示；同一 POI 的别名或重复检索结果合并后再展示。

验证：提交 `781daeb` 部署后，线上行程 `cmsduxe2t001oo10rra095zty` 的每日驾驶避坑按数据库结构化分钟显示 300/221/75/260/332 分钟，未混入跨日路段；8 个景点中重复别名已合并，路线 stop 的精确名称优先保留；14 个 stops 与 13 个 legs 连续。

## ISSUE-019 · bug · P1 · done

用户只写“8月8日至12日”时，日期解析为空，导致自然日驾驶上限、白天驾驶和日期范围校验没有执行。真实回归中因此放行了 D5 418 分钟的返程，虽然页面后来显示了高风险提示，但没有满足用户明确的每日 6 小时约束。

验收：中文和数字格式的无年份日期范围按当前北京时间年份解析；“每天不超过 6 小时驾驶”在此类请求上仍作为硬约束拒绝超限路线，并返回可执行的拆分/加住建议。

实施记录：已增加无年份中文和数字日期范围解析，并增加每日上限回归测试。

验证：新容器内直接执行 `parseTravelDateRange("8月8日至12日")`，在 2026 年参考日期下返回 `2026-08-08` 至 `2026-08-12`、5 天；同一部署的真实旅行行程按北京时间每日自驾 226、173、165、215、307 分钟，全部不超过 360 分钟。

## ISSUE-020 · bug · P1 · done

结构化旅行行程同时从 stops 和 legs 渲染，但创建路径按数组位置绑定 stop ID、按模型文本保存起终点。模型漏写或复用住宿 stop 时，地图链与路线分段会出现不同的起点，天气风险、驾驶分钟和提醒无法确认是否覆盖同一连接。

验收：多 stop 旅行必须有 `stops.length - 1` 段相邻路线；每段起点对应 `stops[i]`，终点对应 `stops[i+1]`；创建和路线替换都按同一 stop 链落盘；不连续输入不得创建半成品行程。

实施记录：已增加服务端端点连续性校验、DeepSeek 修正提示、旅行创建/替换的 canonical stop 映射及单元/集成测试。

验证：提交 `c59a531` 部署到 `192.168.1.56:3002` 后，真实会话 `cmsds0yjw001tlg0s92r62v7w` 创建行程 `cmsds7g68004elg0sm5s0y7tk`。行程为 8 个 stops、7 个 legs；每段 `fromStopId/toStopId` 和名称都对应相邻 stops，地图与路线分段连续性检查通过，未产生半成品行程。

## ISSUE-023 · bug · P1 · done

模型返回自然景点时把 `naturalType` 全部写成 `other`，服务端无法识别草原、湖泊、湿地和火山等实际类型，导致自然景观多样性校验重复失败，完整旅行计划无法落盘。

证据：2026-08-04 线上会话 `cmsdvzo3w00gjo10r35o5ianm` 在 `create_trip` 参数修正后，4 个自然景点的 `naturalType` 均为 `other`；服务端识别为 1 种类型并终止，会话失败且 `tripId=null`，没有半成品行程。

验收：自然景观工具 schema 使用规范英文类型；模型提示禁止 `other`/`unknown`；服务端在模型误填通用类型时，可从景点名称、理由或备注识别至少三种支持充分的自然类型；真实北京→锡林郭勒请求成功落盘，且自然景点实际进入 stops 或明确标为备选。

实施记录：已增加 canonical natural type 枚举、自然类型中英文文本推断和机器可读重试提示，并补充旅行单元测试与规划错误测试。

验证：提交 `cffa250` 已推送到 `violesea/AI-Commute` 的 `agent/travel-planning` 分支并部署到 `192.168.1.56:3002`。真实会话 `cmsdwv8a80001mf0se5optjqz` 在 1 次非法 JSON 工具参数失败后重试成功，行程 `cmsdwzdda001smf0stmj63tdn` 进入 `monitoring`；最终计划包含 `volcanic`、`wetland`、`lake`、`grassland` 四类自然类型和 `cultural` 人文景点，未出现全部归为 `other` 的阻断，且没有半成品行程。

## ISSUE-024 · quality · P1 · open

兼作过渡住宿的自然景观 stop 被路线覆盖识别误判为“备选”，导致用户看见的自然景观主线少于实际路线。

证据：行程 `cmsdwzdda001smf0stmj63tdn` 的结构化 stops 已包含 `太仆寺旗御马苑旅游区` 和 `张北草原`，旅行推荐中的 `naturalType` 均为 `grassland`；但两者 stop `kind` 为 `oversight`，备注含“住宿”，详情页“本次路线覆盖”只显示火山、湿地、达里湖，并把两处草原列为“备选”。这使本次用户明确要求的草原类型在页面上没有被算作已安排，尽管车辆实际经过并在该处过夜。

验收：与自然景点名称或坐标匹配、且确实连接到路线段的景点，即使同时承担过渡住宿或服务区功能，也应标记为“已安排”；只有泛化的城市、酒店或纯住宿 stop 才进入“备选”。覆盖摘要、自然类型数量和景点卡片必须使用同一判断。

## ISSUE-025 · quality · P1 · open

逐段天气风险的路线名称可能与结构化 leg 起终点不一致，天气动作会被用户理解为作用于错误路段。

证据：行程 `cmsdwzdda001smf0stmj63tdn` 的 `travelPlan.weather.routeRisks` 第 5 段写成“锡林浩特-达里湖”，但结构化第 5 段实际为“锡林河国家湿地公园→达里湖”；第 8 段风险写成“锡林浩特-张北草原”，实际为“贝子庙→张北草原”。风险数组数量虽为 9/9，路线标签仍未完全绑定 canonical stops。

验收：服务端创建或展示时按 leg 顺序用结构化 stop 名称生成风险路线标题；模型提供的错误标题只能作为补充说明，不能覆盖真实起终点。每个风险的天气动作、路况建议和提醒必须能追溯到同一个 leg。

## ISSUE-026 · quality · P1 · open

跨自然日的景点路线缺少明确住宿或返程连接，结构化路线连续但住宿语义与模型说明冲突。

证据：同一行程 D2 14:08 到达锡林郭勒草原火山国家地质公园，D3 08:00 又从该景点出发前往锡林河国家湿地公园；住宿推荐只有太仆寺旗、锡林浩特市区和张北，未提供火山公园附近住宿。风险说明却写“傍晚前返回市区”，但 stops/legs 中没有火山公园→锡林浩特市区的返程 leg。用户无法判断 D2 夜间应住景区、回城但未计入路线，还是模型遗漏了一段连接。

验收：若下一自然日从景点继续出发，必须落盘景点附近住宿或在前一日显式加入返回城市的连接段；若模型说明写“返回市区住宿”，结构化路线必须包含对应返程。住宿、stop、leg、天气风险和提醒保持同一可执行路径。

## ISSUE-027 · quality · P2 · done

旅行详情页的交通对比卡片可能继续展示模型生成的自驾总时长，和结构化 stops/legs 的自驾分钟数不一致。

证据：线上行程 `cmsdya07m0028s50spxu23syn` 的结构化路线自驾为 1172 分钟（约 19.5 小时），但“出行决策”摘要写成“全程自驾往返约15.5小时”。结构化路线事实卡片和每日驾驶分钟数本身正确，矛盾来自模型 transport 文案。

验收：旅行详情页在存在结构化路线统计时，交通卡片的自驾总时长只显示 `totalDrivingMinutes`；总理由不得继续复述模型自报的自驾总时长；无结构化路线的普通计划仍保留模型摘要。

实施记录：提交 `23458be` 先将自驾方案摘要改为结构化分钟，提交 `4c1b9c5` 再将旅行交通总理由改为结构化事实提示，并补充 UI 回归断言。

验证：`4c1b9c5` 已推送到 `violesea/AI-Commute` 的 `agent/travel-planning` 并部署到 `192.168.1.56:3002`；线上详情页显示“按已落盘路线，自驾 1172 分钟（约 19.5 小时）”，不再显示“15.5 小时”。

## ISSUE-028 · quality · P1 · open

用户明确优先多种自然类型时，模型可能把其中一种类型只列为备选，主路线没有覆盖用户最关心的自然景观。

证据：行程 `cmsdya07m0028s50spxu23syn` 已安排草原、火山和湿地，湖泊“上都湖”只显示为“备选，不计入本次覆盖”。本轮自然景观数量满足最低校验，但“湖泊”没有进入实际 stops/legs，不能把推荐清单当成已完成路线。

验收：当用户点名且目的地证据支持某自然类型时，规划应优先将至少一个该类型景点写入实际 stops 和关联 legs；若因每日驾驶、天气、开放或路线约束只能放弃，应明确写出放弃原因和可替代方案，并在覆盖摘要中区分已安排与备选。

## ISSUE-029 · data-quality · P2 · open

住宿和美食推荐仍可能出现名称、区域或重复关系未经过服务端一致性校验的问题。

证据：同一行程的美食清单同时出现“上都轩火锅(贝子庙大街店)”和“正蓝旗上都轩火锅/蒙餐”，两项地址均为贝子庙大街与阿尔善路交叉口，但第二项名称含“正蓝旗”且理由仍写锡林浩特市区，疑似重复或区域错配。页面有 POI 证据标记，但证据标记不等于人工核验。

验收：同区域同坐标或高相似名称的住宿/美食推荐应合并或明确为不同门店；名称、地址、推荐理由和行程日必须来自同一 POI 证据，无法确认时降级为区域级建议并提示核验。

## ISSUE-030 · quality · P1 · open

跨城市旅行的天气总览只保存单一城市，可能与实际路线覆盖区域不一致。

证据：本次真实行程 `cmse1le160022th0sx4qhoays` 的结构化 stops 覆盖北京、正蓝旗、锡林浩特和达里诺尔湖（克什克腾旗），但 `travelPlan.weather.city` 为“张家口市”，详情页总览显示“张家口市 暂无天气信息”。10 条逐段风险仍分别绑定路线，未阻断创建，但总览无法代表完整行程。

验收：旅行天气总览应显示多地覆盖或按行程路段列出主要天气点；城市、天气来源和当前预报边界应与实际 stops/legs 对齐。若某地点暂无天气，应明确标为该地点缺失，不把它作为全程总览。

## ISSUE-031 · quality · P1 · open

用户只表达“优先自然风光”时，模型虽然生成了多个自然景观推荐，但没有把足够的自然景点安排进主路线，也没有解释备选原因。

证据：线上行程 `cmse3brsc0022oe0rhtxso8hf` 推荐 4 个自然景观（锡林郭勒大草原、达里诺尔湖、平顶山火山地质公园、锡林河国家湿地公园），实际 stops/legs 只安排达里诺尔湖和平顶山，草原和湿地均显示为“备选 / 顺路可选”；D1-D5 自驾为 326、162、252、168、339 分钟，均未超过用户 360 分钟上限，页面没有给出放弃草原/湿地的明确约束理由。

验收：当用户给出自然风光优先级时，实际主路线应安排足够的自然景观停靠点，并在覆盖摘要中明确已安排与备选；若因天气、开放、路线时长或每日驾驶上限放弃某个自然类型，必须写出原因和替代方案，不能只把推荐清单当成路线结果。
